server: 
  port: 3344 
  
spring:
  application:
    name:  micro-service-cloud-config
  cloud:
    config:
      server:
        git:
          uri: git@github.com:xiuyierxiu/micro-service-cloud-config.git #GitHub上面的git仓库名字
 

